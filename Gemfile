source "https://rubygems.org"

# Roll-your-own iOS + Android release tooling (no EAS / paid service). fastlane bundles `match`
# (iOS signing), `gym`/`pilot`/`deliver` (iOS) and `supply` (Play Store). See docs/RELEASE.md.
gem "fastlane"
gem "cocoapods"
